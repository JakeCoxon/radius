define Range {
  start: int
  end: int
  const __iterate = range_iterate
}

fn range_iterate!(f)(_ range: Range) -> void @inline {
  var i = range.start.copy
  while i < range.end {
    f(i)
    i += 1
  }
}

let ints = Range(start: 0, end: 2147483647)
