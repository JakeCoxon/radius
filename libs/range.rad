type Range @struct:
  start: int
  end: int
  const __iterate = range_iterate

fn range_iterate!(f)(range: Range) -> void @inline:
  let i = range.start
  while i < range.end:
    f(i)
    i += 1

let ints = Range(0, 2147483647)
