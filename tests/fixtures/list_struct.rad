define Thing @struct {
  x : int
  y : int
}

fn foo(thing: Thing, z: int) {
  print(thing.x + z)
}

fn main() {

  let things = [Thing(x: 1, y: 2), Thing(x: 3, y: 2)]
  print(things[0].x + things[1].y)
  
  for thing in things {
    print(thing.x + thing.y)
  }
}