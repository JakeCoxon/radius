
define Thing {
  x : int
  y : int
}

fn foo(_ thing: Thing, z: int) @method {
  print(thing.x + z)
}

fn bam(_ thing: Thing) @method {
  print(thing.x)
}

fn main() {

  var thing = Thing(x: 0, y: 0)

  thing.x = 1
  thing.y = 10
  print(thing.x + thing.y)

  thing.foo(z: 200)
  thing.bam

  const fam = {|x| x + 100}

  var x = 2.fam

}