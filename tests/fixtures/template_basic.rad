fn bamp(x: int) {
  print(x)
}

fn foop!(T)(x: T) -> int {
  12 + x
}

fn bar!(T)() -> int {
  12 + (meta T + 10)
}

fn main() {
  let x = 1 + 3
  let y = 32
  bamp(x: x)
  bamp(x: y)
  bamp(x: y)
  foop!int(x: y)
  foop!int(x: y)

  bar!(123)()
}