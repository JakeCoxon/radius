# Globals can be out of order

let foo1 = Foo(float(2), thing2)

let thing2 = foo()
let thing = 100
let thing3 = meta 1 + 2 + 3

type Foo @struct:
  x: float
  y: int

fn foo() -> int:
  thing += 1
  thing * 10

fn main():
  foo()
  foo()
  thing += 10
  print(thing)
  #print(foo1.y) # TODO
  