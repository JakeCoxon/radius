define Thing {
  let x : int

  const __iterate = iterate
}

fn iterate!(f)(_ thing: Thing) @inline {
  f(32 + thing.x)
}

fn main() {
  let thing = Thing(x: 200)

  for x in thing {
    print(x)
  }
}
